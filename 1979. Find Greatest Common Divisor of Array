class Solution(object):
    def findGCD(self, nums):
        x=min(nums)
        y=max(nums)
        ma=0
        for i in range(1,x+1):
            if(x%i==0 and y%i==0):
                ma=i
        return ma
        