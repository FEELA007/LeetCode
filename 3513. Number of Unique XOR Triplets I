class Solution {
    public int uniqueXorTriplets(int[] nums) {
        if(nums.length<3){
            return nums.length;
        }
         int x = (int) (Math.log(nums.length) / Math.log(2));
        return 1 << (x + 1);
    }
}